{% extends 'base.html.twig' %}

{% block body %}
	{% if editMode %}
		<h1 class="text-center mt-5">Modifier une figure</h1>
	{% else %}
		<h1 class="text-center mt-5">Ajouter une nouvelle figure</h1>
	{% endif %}


	{{ form_start(formTrick) }}

	{{ form_errors(formTrick) }}
	{{ form_row(formTrick.name, {'attr': {'placeholder': "Nom de la figure"}}) }}
	{{ form_row(formTrick.figureGroup, {'attr': {'placeholder': "Groupe auquel appartient la figure"}}) }}
	{{ form_row(formTrick.Description, {'attr': {'placeholder': "Description de la figure"}}) }}
	{{ form_row(formTrick.mainMedia, {'attr': {'placeholder': "Image principale"}}) }}
	{{ form_row(formTrick.media, {'attr': {'placeholder': "Ajouter d'autres images"}}) }}
	{{ form_row(formTrick.video, {'attr': {'placeholder': "Ajouter l'url d'une vidéo"}}) }}


	<button type="submit" class="btn btn-success">
		{% if editMode %}
			Enregistrer les modifications
		{% else %}
			Ajouter la figure
		{% endif %}
	</button>

	{{ form_end(formTrick) }}

{% endblock %}
